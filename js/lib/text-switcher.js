// Generated by CoffeeScript 1.8.0
(function() {
  $(document).ready(function() {
    var $switcherWords, $textContainer, $wordLength, count, doSwitch, switcherWords;
    $textContainer = $('.text-switcher-container');
    $switcherWords = $textContainer.data('switcher-words');
    $switcherWords = $switcherWords.toString();
    $switcherWords = $switcherWords.match(/([^']*)/g);
    switcherWords = [];
    $.each($switcherWords, function(index, item) {
      if (item.length <= 3) {
        $switcherWords.slice(index);
      } else {
        switcherWords.push(item);
      }
    });
    $wordLength = switcherWords.length;
    count = 0;
    doSwitch = function() {
      setTimeout(function() {
        $textContainer.text(switcherWords[count]);
        count++;
        doSwitch();
        if (count > $wordLength) {
          count = 0;
          doSwitch();
        }
      }, 2000);
    };
    doSwitch();
  });

}).call(this);
